import { resolversArticle } from "./article.resolvers";
import { resolversCategry } from "./category.resolvers";
import { resolversUser } from "./user.resolvers";

export const resolvers = [
    resolversArticle,
    resolversCategry,
    resolversUser
]